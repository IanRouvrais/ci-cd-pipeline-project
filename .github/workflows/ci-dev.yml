name: CI - Dev Branch Checks

on:
  pull_request:
    branches: [ main ]

jobs:
  quality-and-security:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        folder: [backend, frontend]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install
        working-directory: ./${{ matrix.folder }}

      - name: Run Linting
        run: npm run lint || echo "Linting bypassed"
        working-directory: ./${{ matrix.folder }}
        continue-on-error: true

      - name: Run NPM Audit
        run: npm audit || echo "Audit bypassed"
        working-directory: ./${{ matrix.folder }}
        continue-on-error: true

      - name: Snyk Security Scan
        uses: snyk/actions/node@master
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=high
          directory: ./${{ matrix.folder }}

      - name: Execute Tests
        run: npm test || echo "Tests bypassed"
        working-directory: ./${{ matrix.folder }}
        continue-on-error: true

      - name: Build Docker Images (Verification)
        run: docker build -t test-image-${{ matrix.folder }} .
        working-directory: ./${{ matrix.folder }}
        continue-on-error: true